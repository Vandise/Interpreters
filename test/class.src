class Person

  //this is all perfectly legal
  x = 1
  x = x + 1
  print(x)

  // classes generally have memeber functions
  func greet(name)
    print("Hello " + name)
  end

  // We can even call methods inside the class body 
  // during its construction
  self.greet("Bob")

end

Person.greet("Ben")